<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EscuelasDeportivas.Pages.EditarDeportePage"
             Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Name="Guardar" Command="{Binding SaveCommand}" Order="Primary" Priority="0" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding Deporte.Nombre}" Placeholder="Nombre del deporte" />
            <Entry Text="{Binding Deporte.Descripcion}" Placeholder="Descripción" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>